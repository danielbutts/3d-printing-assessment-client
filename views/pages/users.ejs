<!DOCTYPE html>
<html>
  <head>
    <title>Trivia Scorecard</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Users</h1>
    <% users.forEach((user) => { %>
      <div><%= user.name %></div>
    <% }); %>
  </body>
</html>
